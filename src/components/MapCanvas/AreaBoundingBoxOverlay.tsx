import React from 'react';
import * as maplibregl from 'maplibre-gl';

// Clean overlay — all text labels on area shapes are removed as requested by the user
export const AreaBoundingBoxOverlay: React.FC<{ map: maplibregl.Map | null }> = () => {
  return null;
};
